apiVersion: v1
kind: Service
metadata:
  name: alert-processor-service
  namespace: safealert-app
spec:
  selector:
    app: alert-processor-service
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087
  type: ClusterIP