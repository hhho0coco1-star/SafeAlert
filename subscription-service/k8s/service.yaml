apiVersion: v1
kind: Service

metadata:
  name: subscription-service
  namespace: safealert-app

spec:
  selector:
    app: subscription-service
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
  type: ClusterIP